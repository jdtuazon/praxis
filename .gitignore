# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Environment / secrets
.env
.env.local
*.key

# Praxis runtime state (persistent memory DB lives here by default).
# Kept out of git — memory is per-user runtime state, not source.
.praxis/
*.sqlite
*.sqlite3
*.db

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# Local-only assessment/scoring harness (a dev loop tool, not part of the app).
assessment/

# Scratch / tooling
tmp/
tmp_*.json
.playwright-mcp/
*.png
